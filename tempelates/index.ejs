<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/style/index.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
    integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.9.1/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.9.1/ScrollTrigger.min.js"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
<div class="welcome">
    <%= collection.type %>
</div>
    <main>
        <nav>
            <div class="logo">Stack Hub</div>
            <div class="search">
                <form action="/search" method="GET">
                    <input type="text" name="q" placeholder="Enter Username/Code" required>
                    <button type="submit">Search</button>
                </form>
                
            </div>
            <div class="acc">
                <% if (userId) { %>
                    <a href="/auth/logout" class="logout"><i class="fa-solid fa-right-from-bracket"></i>Log Out</a>
                    <% } else { %>
                        <a href="/auth/signup" class="sign"><i class="fa-solid fa-right-to-bracket"></i><p>Sign Up</p></a>
                        <a href="/auth/login" class="login"><i class="fa-solid fa-lock"></i><p>Log In</p></a>
                        <% } %>
            </div>
        </nav>

        <div class="hero">
            <div class="div">
                <div class="left">
                    <h3>Create & share your Collections</h3>
                    <p>Save your favorite songs and images in personalized collections. Share them with others or keep them just for yourself—it's all up to you!</p>
                    <div class="btns">
                        <button><i
                            class="fa-solid fa-arrow-up-right-from-square"></i>Get Started</button>
                    </div>
                </div>
                <div class="right">
                    <img src="/img/hero.png" alt="hero-img">
                </div>
            </div>
        </div>

        <section id="collections" class="section">
            <h2>My Collections</h2>
            <div class="bar">
                <div class="filters">
                    <a href="" class="active">Show all</a>
                    <a href="">Music</a>
                    <a href="">Images</a>
                </div>
                <div class="add">
                    <h2>+ Create New</h2>
                </div>
            </div>
            <div class="list">
                <% collections.forEach(collection=> { %>
                    <div class="collec">
                        <h2>

                            <%= collection.type %>
                        </h2>
                        <h1>
                            <%= collection.name %>
                        </h1>
                        <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Praesentium aperiam illo laboriosam 
                        </p>
                        <a href="/show-collection/<%= collection._id %>"><i
                            class="fa-solid fa-arrow-up-right-from-square"></i></a>

                            
                            <form action="/delete-collection/<%= collection._id %>" method="POST" class="delete-form">
                                <button type="submit" onclick="return confirm('Are you sure you want to delete this collection?');">
                                    <i class="fa-solid fa-trash"></i> Delete
                                </button>
                            </form>
                    </div>
                    <% }) %>
            </div>
        </section>

        <footer>
            <p>Developed By ANSHAL & SHARIKH</p>
        </footer>
    </main>

</body>

<script>
    const addCollection = document.querySelector('.add');
    addCollection.addEventListener('click', () => {
        window.location.href = "/add-collection";
    })
</script>
<script src="/script/index.js"></script>
</html>